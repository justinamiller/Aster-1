// Aster Standard Library
// Main module entry point
//
// This is the root of the Aster standard library.
// Import this module to get access to all stdlib functionality.
//
// The stdlib is organized into 13 layered modules:
// 1. core   - Primitives, no alloc, no IO
// 2. alloc  - Heap allocation
// 3. collections - Collection data structures
// 4. sync   - Concurrency primitives
// 5. io     - I/O operations
// 6. fs     - Filesystem
// 7. net    - Networking
// 8. time   - Time and duration
// 9. fmt    - Formatting
// 10. math   - Mathematics
// 11. testing - Test framework
// 12. env   - Environment
// 13. process - Process control
//
// Stability: Mixed (see individual modules)
// Effects: Varies by module

// Re-export all stdlib modules
pub mod core;
pub mod alloc;
pub mod collections;
pub mod sync;
pub mod io;
pub mod fs;
pub mod net;
pub mod time;
pub mod fmt;
pub mod math;
pub mod testing;
pub mod env;
pub mod process;

// Prelude - commonly used types and traits
// Import this for the most common stdlib items
pub mod prelude {
    // Core types
    pub use core::{
        // Option and Result
        Option, Some, None, some, none,
        Result, Ok, Err, ok, err,
        
        // Core traits
        Copy, Clone, Drop,
        Eq, Ord, Hash,
        Default, Display, Debug,
        Iterator, IntoIterator,
        From, Into,
        
        // Utilities
        panic, assert, assert_eq, assert_ne
    };
    
    // Allocation
    pub use alloc::{Vec, String, Box, new_vec, new_string, new_box};
    
    // Collections
    pub use collections::{HashMap, HashSet, LinkedList, new_hashmap, new_hashset, new_list};
    
    // Formatting
    pub use fmt::{print, println, eprint, eprintln};
}
