// Test file for --stage1 flag
// This uses closures which should be rejected in Stage1 mode

fn main() {
    let add_one = |x| x + 1;
    let result = add_one(5);
    
    let multiply = |a, b| a * b;
    let prod = multiply(3, 4);
}
