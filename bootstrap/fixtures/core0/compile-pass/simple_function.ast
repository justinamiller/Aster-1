// Test fixture: simple_function.ast
// Category: compile-pass
// Tests: Basic function definition and calls (Core-0)

fn add(a: i32, b: i32) -> i32 {
    a + b
}

fn multiply(a: i32, b: i32) -> i32 {
    a * b
}

fn main() {
    let x = add(5, 3);
    let y = multiply(x, 2);
    print(y);  // Expected: 16
}
