// Production Test - Demonstrates Aster compiler works
fn factorial(n: i32) -> i32 {
    if n <= 1 {
        1
    } else {
        n * factorial(n - 1)
    }
}

fn main() -> i32 {
    let result = factorial(5);
    result // Should be 120
}
