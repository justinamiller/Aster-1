// Fibonacci calculator
// Demonstrates recursive functions and integer arithmetic

fn fib(n: i32) -> i32 {
    if n <= 1 {
        n
    } else {
        fib(n - 1) + fib(n - 2)
    }
}

fn main() {
    let result = fib(10)
    print("Fibonacci(10) = ")
    print_int(result)
}
